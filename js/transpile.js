// Generated by CoffeeScript 1.11.1
var assert, spacesToTabs, transpile;

assert = function(a, b) {
  if (b == null) {
    b = true;
  }
  if (!_.isEqual(a, b)) {
    return print(JSON.stringify(a) + " != " + JSON.stringify(b));
  }
};

spacesToTabs = function(line) {
  if (line.indexOf('  ') === 0) {
    return '\t' + spacesToTabs(line.substring(2));
  }
  if (line.indexOf('\t') === 0) {
    return '\t' + spacesToTabs(line.substring(1));
  }
  if (line.indexOf(' \t') === 0) {
    return '\t' + spacesToTabs(line.substring(2));
  }
  return line;
};

assert(spacesToTabs('    '), '\t\t');

assert(spacesToTabs('\t  '), '\t\t');

assert(spacesToTabs('  \t'), '\t\t');

assert(spacesToTabs(' \t  '), '\t\t');

transpile = function(code) {
  var line, lines, temp;
  lines = code.split('\n');
  temp = (function() {
    var i, len, results;
    results = [];
    for (i = 0, len = lines.length; i < len; i++) {
      line = lines[i];
      results.push(spacesToTabs(line));
    }
    return results;
  })();
  code = temp.join('\n');
  return CoffeeScript.compile(code, {
    bare: true
  });
};
